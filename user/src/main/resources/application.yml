server:
  port: 8080
spring:
  application:
    name: user
## 配置数据源
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.209.129:5432/twp_db?characterEncoding=utf-8
    username: postgres
    password: 123456
    type: com.alibaba.druid.pool.DruidDataSource
#    datasource:
#      driver-class-name: com.mysql.cj.jdbc.Driver
#      url: jdbc:mysql://192.168.209.129:3306/bfsh?useUnicode=true&characterEncoding=utf-8
#      username: root
#      password: 123456
#      type: com.alibaba.druid.pool.DruidDataSource
## 配置rabbitmq
  rabbitmq:
    host: 192.168.209.129
  ## 配置redis
  redis:
    host: 192.168.209.129
#  security:
#    user:
#      name: admin
#      password: admin
eureka:
  client:
    service-url:
      defaultZone: http://172.25.252.182:8761/eureka
  instance:
    prefer-ip-address: true

# mybatis-plus相关配置
# xml扫描，多个目录用逗号或者分号分隔（告诉 Mapper 所对应的 XML 文件位置）
#数据库类型
#主键类型 AUTO:"数据库ID自增" INPUT:"用户输入ID",ID_WORKER:"全局唯一ID (数字类型唯一ID)", UUID:"全局唯一ID UUID";
#字段策略 IGNORED:"忽略判断"  NOT_NULL:"非 NULL 判断")  NOT_EMPTY:"非空判断"
# 是否开启自动驼峰命名规则映射:从数据库列名到Java属性驼峰命名的类似映射
# 如果查询结果中包含空值的列，则 MyBatis 在映射的时候，不会映射这个字段
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  global-config:
    db-config:
      db-type: postgre_sql
      id-type: input
      field-strategy: not_empty
  configuration:
    map-underscore-to-camel-case: true
    call-setters-on-nulls: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl




